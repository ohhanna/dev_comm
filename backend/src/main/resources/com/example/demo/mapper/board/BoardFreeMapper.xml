<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<!-- <mapper namespace="com.example.demo.mapper.board.BoardFreeMapper"> 
    <select id="selectTest" resultType="com.example.demo.vo.board.BoardFreeVo"> 
        SELECT 'GOD' AS NAME 
    </select> 
</mapper> -->

<mapper namespace="com.example.demo.mapper.board.BoardFreeMapper"> 
    <select id="BoardFreeList" resultType="com.example.demo.vo.board.BoardFreeVo"> 
        SELECT * FROM BOARD_FREE 
            LIMIT #{limit} 
            OFFSET #{offset}
    </select> 
</mapper>